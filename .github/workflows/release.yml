name: Release

# Dispara em push de tag v*.*.* (ex: v3.0.1, v3.1.0).
# Builda Windows + Linux em paralelo, publica no GitHub Releases.
on:
  push:
    tags:
      - 'v*.*.*'

permissions:
  contents: write

jobs:
  release:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [windows-latest, ubuntu-latest]
    defaults:
      run:
        working-directory: ./SoftHair

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node 20
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: SoftHair/package-lock.json

      - name: Install root deps
        run: npm install --legacy-peer-deps --ignore-scripts

      - name: Install backend deps (production)
        run: npm install --omit=dev
        working-directory: ./SoftHair/backend

      - name: Install frontend deps
        run: npm install --legacy-peer-deps
        working-directory: ./SoftHair/frontend

      - name: Build frontend
        run: npm run build:frontend

      - name: Build & publish (Windows)
        if: matrix.os == 'windows-latest'
        run: npx electron-builder --win --publish=always
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Build & publish (Linux)
        if: matrix.os == 'ubuntu-latest'
        run: npx electron-builder --linux --publish=always
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
